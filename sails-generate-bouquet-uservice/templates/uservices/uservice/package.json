{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t <%= projectName %>/<%= name %>",
    "build": "docker push localhost:5000/<%= projectName %>/<%= name %>"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
