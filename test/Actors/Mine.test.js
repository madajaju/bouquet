var Promise = require('bluebird');

describe('Actor Mine', function () {
  describe('Primary UseCase', function () {
    it('Primary Scenario', function (done) {
                done();
    });
  });
});
