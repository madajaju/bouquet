
version: '3'

services:
  # mongo:
  #   image: mongo
  #   expose:
  #        - 27017
  #   ports:
  #        - "27017:27017"
  bouquet:
    image: bouquet/web
    expose:
      - 1337
    ports:
      - "1337:1337"
  # uservice-name :
  #  image: bouquet/uservice-name
  #  deploy:
  #          mode: replicated
  #          replicas: 5
